# FlexiView 依赖包

# Web Server
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# 核心依赖
opencv-python>=4.8.0
numpy>=1.24.0
screeninfo>=0.8.1
Pillow>=10.0.0

# 红外摄像头支持 (仅 Windows 平台)
# 如果需要使用红外摄像头功能，请在 Windows 平台安装以下包。
# 使用环境标记确保在 Linux/macOS 上自动跳过（避免构建失败）。
winrt-Windows.Devices.Enumeration; platform_system == "Windows"
winrt-Windows.Media.Capture; platform_system == "Windows"
winrt-Windows.Media.Capture.Frames; platform_system == "Windows"
winrt-Windows.Graphics.Imaging; platform_system == "Windows"
winrt-Windows.Foundation; platform_system == "Windows"
winrt-Windows.Foundation.Collections; platform_system == "Windows"
winrt-Windows.Storage.Streams; platform_system == "Windows"

# 可选：如果需要更多视频格式支持
# opencv-contrib-python>=4.8.0
